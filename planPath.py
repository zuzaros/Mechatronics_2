
#this function is used to plan the path of babyspice
def plan_path(map_grid):
    """
    plan_path(map_grid)
    Plan the path of BabySpice to collect all the spice and return to the starting point.
    Args:
        map_grid (numpy.ndarray): A 2D NumPy array representing the grid map.
    Returns:
        None
    """
    # Implement the path planning algorithm here
    print("Calculate the path of BabySpice to collect all the spice and return to the starting point.")